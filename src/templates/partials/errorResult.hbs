{{~#if errorResults~}}
{{#each results}}ApiError<
	{{#equals code 0}}number{{else}}{{{code}}}{{/equals}},
	{{type}}
>{{#unless @last}} | {{/unless}}{{/each}}
{{~else~}}
ApiError<number, any>
{{~/if~}}
